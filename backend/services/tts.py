"""Génération audio à partir d'un texte.

TTS pas encore choisi (cf spec: edge-tts gratuit ou API externe). Pour l'instant
ce service ne fait rien et renvoie None — le texte seul est utilisable côté
frontend en attendant l'intégration TTS.
"""


def generer_audio(texte: str) -> str | None:
    return None
